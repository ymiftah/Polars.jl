# Metadata

```@setup metadata
using Polars
```

```@docs
Polars.version
```

```julia
Polars.version()  # => v"0.54.4" or similar
```

`version()` is not exported — call it qualified, `Polars.version()`.
