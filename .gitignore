Manifest.toml
docs/node_modules/
docs/build/
.claude/

# Repo-root scratch directory for local experiments; anchored so it can't swallow a real
# `debug/` directory nested anywhere else in the tree.
/debug/
